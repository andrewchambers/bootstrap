# bootstrap

Small, standalone bootstrap for producing a self-hosted x86_64 Linux musl toolchain archive.

```sh
./bootstrap.sh
```

The build writes `bootstrap.tar.xz`, `bootstrap.tar.gz`, and verifies them against `bootstrap.sha256`.
